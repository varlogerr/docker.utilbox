# docker.utilbox
```
mkdir -p ~/.bin && cd $_ \
  && git clone https://github.com/varlogerr/docker.utilbox.git \
  && cp -R ./docker.utilbox/docker/_assets/. ./ \
  && echo "export PATH=\$PATH:$(pwd)" >> ~/.bashrc
```
